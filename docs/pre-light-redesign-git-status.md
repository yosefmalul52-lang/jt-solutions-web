# מצב Git לפני Light Premium Redesign

**תאריך:** 26 ביוני 2026  
**מטרה:** שמירת snapshot לפני שינוי שפה עיצובית גלובלי (בהיר במקום כהה)

---

## סיכום

| מדד | ערך |
|-----|-----|
| קבצים changed (modified/deleted) | **54** |
| קבצים untracked (חדשים) | **83** |
| **סה״כ שורות בסטטוס** | **137** |

---

## Changed (modified / deleted)

קבצים שסומנו כ־`M` או `D` ב־git — כוללים עמודים, קומפוננטות, lib, ומחיקות dead code (למשל `GlassCard.tsx`, `Proof.tsx`).

## Untracked (חדשים)

83 קבצים שלא ב־git — כוללים:

- קומפוננטות חדשות (`components/home/`, `StudioServiceTemplate`, סקשני שירות)
- דוחות `docs/*`
- דוחות Lighthouse `.lighthouse-reports/`
- `lib/studio-shell.ts`, `lib/services-*-page.ts` ועוד

---

## סיכון לאבד עבודה

| סיכון | רמה | הסבר |
|-------|-----|------|
| עבודה לא committed | **גבוה** | כל השדרוגים האחרונים (עיצוב כהה, עמודי שירות, טופס, SEO) קיימים רק בעותק מקומי |
| `git reset` / rollback אגרסיבי | **גבוה** | עלול למחוק שינויים שלא נשמרו ב-commit |
| שינוי עיצוב בלבד | **נמוך** | אם נשנה רק CSS/קלאסים בלי reset — העבודה הקיימת נשמרת |

**המלצה:** לא לבצע `git reset`, לא למחוק קבצים, ולשנות עיצוב בזהירות על בסיס הקוד הנוכחי.

---

*נוצר אוטומטית לפני שלב 0 של Light Premium Redesign.*
