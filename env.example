# Admin gate
ADMIN_PASSWORD=demo1233

# Contact notifications
SMTP_USER=
SMTP_PASS=

# Supabase — REQUIRED for admin dashboard reads/writes:
# Project Settings → API → service_role (secret, server-only)
NEXT_PUBLIC_SUPABASE_URL=https://ohkghyxytzcjowzosfna.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Used by the public contact form when service role is not set (submit_site_lead RPC).
SUPABASE_ANON_KEY=

# Rate limiting on Vercel: use Firewall (no Redis/Upstash).
# Live rule on this project: "Rate limit login+contact"
#   POST /api/admin/login OR POST /api/contact → 20 req / 10 min / IP
# Manage: vercel firewall rules list | Vercel Dashboard → Firewall

# Optional booking CTA in admin
NEXT_PUBLIC_BOOKING_URL=

# Analytics (loaded only after cookie consent)
NEXT_PUBLIC_FB_PIXEL_ID=1070553158866028
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_GTM_ID=

# Google Search Console (optional meta-tag verification; HTML file already in public/)
# Get the content value from GSC → Ownership verification → HTML tag
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Meta Lead Ads webhook (app/api/meta/leads)
# Webhooks → Page object → leadgen field
META_VERIFY_TOKEN=
META_APP_ID=
META_APP_SECRET=
META_PAGE_ID=
META_PAGE_ACCESS_TOKEN=
