/**
 * Single CSS-only vertical canvas for the homepage funnel.
 * Sits behind all sections; sections stay transparent on top.
 */
export default function HomepageBackgroundStory() {
  return <div className="homepage-background-story" aria-hidden />;
}
